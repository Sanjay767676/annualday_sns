kind = "web"
previewPath = "/"
title = "Dynamic Form System"
version = "1.0.0"
id = "artifacts/forms-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19961

[services.development]
run = "pnpm --filter @workspace/forms-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/forms-app", "run", "build" ]
publicDir = "artifacts/forms-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19961"
BASE_PATH = "/"
