{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "lib/*",
    "artifacts/*",
    "scripts"
  ],
  "scripts": {
    "postinstall": "npm run -w @workspace/db prisma:generate",
    "build": "npm run typecheck && npm run -w @workspace/api-server build && npm run -w @workspace/forms-app build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run -w @workspace/forms-app typecheck && npm run -w @workspace/api-server typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
